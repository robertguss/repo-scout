# Phase 7 semantic precision fixture

The Phase 7 semantic precision fixture intentionally contains duplicate callee names across
TypeScript and Python modules.

It is used by milestone tests and dogfood command packs to validate module-aware call resolution and
deterministic ranking behavior.
